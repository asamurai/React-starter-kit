import { createRequestTypes } from './../utils';

export const COUNT_ADD = createRequestTypes('COUNT_ADD');
export const COUNT_REMOVE = createRequestTypes('COUNT_REMOVE');
export const COUNT_CLEAR = createRequestTypes('COUNT_CLEAR');
